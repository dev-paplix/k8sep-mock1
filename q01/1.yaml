apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment
  namespace: qq2
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx-app
    
  template:
    metadata:
      labels:
        app: nginx-app
    spec:
      containers:
      - name: nginx-cont
        image: nginx